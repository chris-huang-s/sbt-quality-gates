name: Quality Gates
description: Run sbt qualityGates (Scalafmt, Scalafix, test) with JDK setup and caching.
author: chris-huang-s

inputs:
  java-version:
    description: Temurin JDK major version used for the quality gate run
    required: false
    default: "21"
  working-directory:
    description: Directory containing the sbt build (relative to the repository root)
    required: false
    default: "."
  sbt-args:
    description: Extra arguments passed to sbt before the qualityGates task (for example -batch)
    required: false
    default: ""

runs:
  using: composite
  steps:
    - name: Set up JDK
      uses: actions/setup-java@v6
      with:
        distribution: temurin
        java-version: ${{ inputs.java-version }}
        cache: sbt

    - name: Run quality gates
      shell: bash
      working-directory: ${{ inputs.working-directory }}
      run: sbt ${{ inputs.sbt-args }} qualityGates
